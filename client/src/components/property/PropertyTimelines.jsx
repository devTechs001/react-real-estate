import Card from '../ui/Card';

const PropertyTimelines = ({ property }) => {
  return (
    <Card>
      <h2 className="text-2xl font-bold mb-4">PropertyTimelines</h2>
      <p className="text-gray-600">Property information will be displayed here.</p>
    </Card>
  );
};

export default PropertyTimelines;
